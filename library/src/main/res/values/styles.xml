<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="SelectImageTransparent">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowFrame">@null</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowNoTitle">false</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="CropActionButtonTextCancel" parent="Crop.ActionButtonText.Cancel">
        <item name="android:textColor">@color/sp_crop_button_text</item>
        <item name="android:drawableLeft">@drawable/sp_ic_close_grey_24dp</item>
    </style>

    <style name="CropActionButtonTextDone" parent="Crop.ActionButtonText.Done">
        <item name="android:textColor">@color/sp_crop_button_text</item>
        <item name="android:drawableLeft">@drawable/sp_ic_done_grey_24dp</item>
    </style>
</resources>